namespace FolketingetVotes.Core.Enums;

/// <summary>AktørAktørRolle values used by this application (subset of the API's list).</summary>
public enum ActorRelationRole
{
    Minister = 8,
    Substitute = 10,
    ViceChair = 14,
    Member = 15,
    Chair = 16,
}
