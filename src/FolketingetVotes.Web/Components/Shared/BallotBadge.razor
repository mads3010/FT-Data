<span class="ballot @Labels.BallotCss(Type)">@(Short ? Labels.BallotShort(Type) : Labels.Ballot(Type))</span>

@code {
    [Parameter] public BallotType Type { get; set; }
    [Parameter] public bool Short { get; set; }
}
